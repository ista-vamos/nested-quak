# Probe Sources

This directory contains optional investigation and comparison tools. They are
not registered as CTest tests.

Some probes are still built by the `experiments` CMake target so historical
backend comparisons remain available without mixing them with correctness and
sanity tests.
